#include <stdio.h>
#include <stdlib.h>
#include "node.h"

int main() {
	Node first = nodeCreate(1);
	Node next1 = nodeCreate(2);
	nodeSetNext(first, next1);
	//Add code for checking rest of functions,
	//and destroying nodes.
	//Also add comments.
}